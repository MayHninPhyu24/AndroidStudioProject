import 'package:flutter/material.dart';

class A16Another extends StatelessWidget {
  const A16Another({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Another Screen'),),
      body: null,
    );
  }
}
